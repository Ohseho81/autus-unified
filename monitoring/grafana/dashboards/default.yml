# monitoring/grafana/dashboards/default.yml
# 대시보드 자동 로드 설정

apiVersion: 1

providers:
  - name: 'default'
    orgId: 1
    folder: 'AUTUS'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards
